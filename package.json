{
  "name": "live-location-tracking",
  "version": "1.0.0",
  "description": "A live location tracking website with user authentication",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "build": "echo \"No build step required for Node.js app\"",
    "test": "echo \"No tests specified\" && exit 0",
    "clean": "rm -rf node_modules package-lock.json && pnpm install",
    "setup": "pnpm install && echo \"Setup complete! Run 'pnpm start' to begin.\""
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.1.3",
    "twilio": "^4.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
